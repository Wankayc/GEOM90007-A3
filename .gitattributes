# GEOM90007 Assignment 3 - Git LFS Configuration
# This file automatically tracks large files with Git LFS

# SPATIAL DATA FILES
data/raw/*.geojson filter=lfs diff=lfs merge=lfs -text
*.geojson filter=lfs diff=lfs merge=lfs -text
*.gpkg filter=lfs diff=lfs merge=lfs -text

# R DATA FILES  
*.rds filter=lfs diff=lfs merge=lfs -text
*.RData filter=lfs diff=lfs merge=lfs -text

# DATA & OUTPUT FILES
output/**/* filter=lfs diff=lfs merge=lfs -text
results/**/* filter=lfs diff=lfs merge=lfs -text

# EXCLUDE CSV FILES FROM LFS (so they work in zip downloads)
*.csv text
data/raw/weather_and_air/*.csv text

# Only track large files in data/raw, not everything
data/raw/Public*.geojson filter=lfs diff=lfs merge=lfs -text
